# 読み取り専用通常運転ランタイムの実サーバー検証

## 検証対象

- 基準コミット: `42d4cddcc37a979dd76ace6a8281345b5b723793`
- 実施日: 2026-07-30
- 環境: 接続許可を得た専用テストサーバー

サーバー接続情報、BOTアカウント名、実プレイヤー名、認証情報は記録しません。

## 結果

| 試験 | 内容                                                    | 結果 |
| ---- | ------------------------------------------------------- | ---- |
| A    | BOT単独で接続・スポーン後、2分以上維持し、SIGTERMで終了 | 合格 |
| B    | BOT接続後の他プレイヤー参加を検知して安全切断           | 合格 |
| C    | 他プレイヤー先行時、スポーン時の初期一覧検査で安全切断  | 合格 |

全試験で`outcome`は`normal`、`exitCode`は`0`でした。予期しない再接続、多重接続、
Minecraft内操作はありませんでした。試験BとCでは検知と切断が各1回で、安全停止後の
`reconnect.*`はありませんでした。認証volumeは試験前後で維持されました。

## 受入判断

読み取り専用通常運転について、単独常駐、SIGTERM停止、後発プレイヤー検知、
初期プレイヤー検査、安全停止後の非再接続を専用テストサーバーで確認済みです。
この結果は状態・進捗管理の設計基準であり、Minecraft内操作を承認するものではありません。
